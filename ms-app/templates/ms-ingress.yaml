apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: hello-ms-ingress
  annotations:
    kubernetes.io/spec.ingressClassName: nginx
spec:
  rules:
  - host: www.hafiz-tech.com
    http:
      paths:
      - path: /hello-ms
        pathType: Prefix
        backend:
          service:
            name: hello-ms-service
            port:
              number: 80